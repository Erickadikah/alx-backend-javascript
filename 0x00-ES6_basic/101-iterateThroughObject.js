export default function iterateThroughObject(reportWithIterator) {
	const object = [];
	reportWithIterator.forEach((value, index) => {
		if (index < reportwithIterator.leght - 1) {
			name+= `${value} |`;
		} else {
			name += value;
		}
	});
	return names;
 }
